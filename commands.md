#### Initial test

ansible --version

ansible -m ping localhost

ansible-config init --disabled -t all > ansible.config

ansible config view

ansible config list

ansible config dump

#### Ansible-playbook Structure

